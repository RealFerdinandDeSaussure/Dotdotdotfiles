/* %-LOCATION:$HOME/.config/ironbar/style.css-% -*-css-*- */
@define-color base00 #{{{$__BASE00}}};
@define-color base01 #{{{$__BASE01}}};
@define-color base02 #{{{$__BASE02}}};
@define-color base03 #{{{$__BASE03}}};
@define-color base04 #{{{$__BASE04}}};
@define-color base05 #{{{$__BASE05}}};
@define-color base06 #{{{$__BASE06}}};
@define-color base07 #{{{$__BASE07}}};
@define-color base08 #{{{$__BASE08}}};
@define-color base09 #{{{$__BASE09}}};
@define-color base0a #{{{$__BASE0A}}};
@define-color base0b #{{{$__BASE0B}}};
@define-color base0c #{{{$__BASE0C}}};
@define-color base0d #{{{$__BASE0D}}};
@define-color base0e #{{{$__BASE0E}}};
@define-color base0f #{{{$__BASE0F}}};

:root {
    --wh-size: 4px;
    --wh-alpha: 0.55;
    --bar-pad-vert: 0.37rem;
    --button-pad-vert: 0.28em;
    --popup-pad-vert: 0.5em;
    --popup-pad-horiz: 1em;
    --widget-pad-horiz: 0.65em;
    --base00-rgb: 50,48,47;
    --base01-rgb: 60,56,54;
    --base02-rgb: 80,73,69;
    --base03-rgb: 102,92,84;
    --base04-rgb: 189,174,147;
    --base05-rgb: 213,196,161;
    --base06-rgb: 235,219,178;
    --base07-rgb: 251,241,199;
    --base08-rgb: 251,73,52;
    --base09-rgb: 254,128,25;
    --base0a-rgb: 250,189,47;
    --base0b-rgb: 184,187,38;
    --base0c-rgb: 142,192,124;
    --base0d-rgb: 131,165,152;
    --base0e-rgb: 211,134,155;
    --base0f-rgb: 214,93,14;
}

* {
    border: none;
    border-radius: 0;
    margin: 0;
    background-image: none;
    font-family: sans-serif, "FontAwesome";
    font-size: 1.1rem;
    padding: 0;
    transition: none;
    border-color: transparent;
    border-style: solid;
    box-shadow: none;
}

.background {
    background: transparent;
}

#bar {
    background: linear-gradient(to right, rgba(var(--base02-rgb),.8), rgba(var(--base00-rgb),.9) 33%, rgba(var(--base01-rgb),.9));
    color: @base06;
}

.widget {
    padding-bottom: var(--bar-pad-vert);
    padding-top: var(--bar-pad-vert);
    color: inherit;
}

.widget-box {
    padding: 0;
}

button {
    color: inherit;
}

#bar #end revealer .widget {
    padding-left: var(--widget-pad-horiz);
    padding-right: var(--widget-pad-horiz);
    margin-left: 0.2em;
    margin-right: 0.2em;
}

#bar #end .widget:hover {
    background-position: left;
    color: @base00;
}

#bar #end revealer:nth-child(odd) > .widget {
    background:
        right
        no-repeat
        linear-gradient(to right,
                        rgba(var(--base0c-rgb),var(--wh-alpha)) calc(50% + var(--wh-size)),
                        transparent calc(50% + var(--wh-size)));
    background-size: 200% 1.2em;
    transition: color 500ms ease, background-size 200ms ease, background-position 300ms ease;
}

#bar #end revealer:nth-child(odd) .widget:hover {
    background:
        right
        no-repeat
        linear-gradient(to right,
                        rgba(var(--base0c-rgb),1) calc(50% + var(--wh-size)),
                        transparent calc(50% + var(--wh-size)));
    background-position: left;
    background-size: 200% 100%;
    color: @base00;
}

#bar #end revealer:nth-child(even) > .widget {
    background:
        right
        no-repeat
        linear-gradient(to right,
                        rgba(var(--base0d-rgb),var(--wh-alpha)) calc(50% + var(--wh-size)),
                        transparent calc(50% + var(--wh-size)));
    background-size: 200% 1.2em;
    transition: color 500ms ease, background-size 200ms ease, background-position 300ms ease;
}

#bar #end revealer:nth-child(even) .widget:hover {
    background:
        right
        no-repeat
        linear-gradient(to right,
                        rgba(var(--base0d-rgb),1) calc(50% + var(--wh-size)),
                        transparent calc(50% + var(--wh-size)));
    background-position: left;
    background-size: 200% 100%;
    color: @base00;
}

contents {
    background-color: transparent;
}

tooltip box {
    background-color: @base01;
}

.popup {
    border-radius: 4px;
}

#bar #end revealer:nth-child(odd) .popup {
    padding: var(--popup-pad-vert) var(--popup-pad-horiz);
    background: radial-gradient(circle at 80% 120%, @base0c, @base03 70%, @base01);
    color: @base07;
}

#bar #end revealer:nth-child(even) .popup {
    padding: var(--popup-pad-vert) var(--popup-pad-horiz);
    background: radial-gradient(circle at 80% 120%, @base0d, @base03 70%, @base01);
    color: @base07;
}

.popup .text-button,
.popup .image-button {
    padding-top: var(--button-pad-vert);
    padding-bottom: var(--button-pad-vert);
    border-radius: 4px;
    transition: background, color;
    transition-duration: 200ms;
    transition-timing-function: ease;
}

.popup .text-button:hover,
.popup .image-button:hover {
    color: @base00;
}

#bar #end revealer:nth-child(odd) .popup .text-button:hover,
#bar #end revealer:nth-child(odd) .popup .image-button:hover {
    background: @base0c;
}

#bar #end revealer:nth-child(even) .popup .text-button:hover,
#bar #end revealer:nth-child(even) .popup .image-button:hover {
    background: @base0d;
}

/* Styling of individual widgets and their popups */

.bindmode {
    color: @base00;
    padding-left: 2em;
    padding-right: 2em;
    background: linear-gradient(to right, transparent, @base0a 2em, @base0a calc(100% - 2em), transparent);
}

.clipboard-popup {
    min-width: 300px;
}

.focused .label {
    font-weight: 700;
    font-stretch: expanded;
}

.popup-music {
    min-width: 25rem;
    max-width: 30rem;
}

.popup-music .title .label {
    font-weight: 700;
    font-size: 1.3em;
}

.popup-music .album .label { font-style: italic; }
.popup-music * .icon-box {
    margin-right: 1em;
    min-width: 1.4rem;
    max-width: 1.4rem;
}

.popup-music .controls button {
    margin: 0.2em 0.6em;
    padding: 0.2em 0.8em;
    color: @base00;
}

.popup-music .volume .icon {
    color: @base00;
}

.popup-music .volume .slider trough {
    background: linear-gradient(to top, @base06 40%, rgba(var(--base06-rgb), 0.1) 95%);
    border-radius: 6px 6px 0px 0px;
}

.popup-music .progress .label {
    color: @base00;
}

.popup-music .progress .slider trough {
    background: linear-gradient(to right, @base06 40%, rgba(var(--base06-rgb), 0.1) 95%);
    border-radius: 0px 6px 6px 0px;
}

#bar #end revealer:nth-child(even) .popup-music .volume .slider trough highlight {
    background: linear-gradient(to top,
                                rgba(var(--base0d-rgb),1) 50%,
                                rgba(var(--base07-rgb),1));
}

#bar #end revealer:nth-child(even) .popup-music .progress .slider trough highlight {
    background: linear-gradient(to right,
                                rgba(var(--base0d-rgb),1) 50%,
                                rgba(var(--base07-rgb),1));
}

#bar #end revealer:nth-child(odd) .popup-music .volume .slider trough highlight {
    background: linear-gradient(to top,
                                rgba(var(--base0c-rgb),1) 50%,
                                rgba(var(--base07-rgb),1));
}

#bar #end revealer:nth-child(odd) .popup-music .progress .slider trough highlight {
    background: linear-gradient(to right,
                                rgba(var(--base0c-rgb),1) 50%,
                                rgba(var(--base07-rgb),1));
}

.popup-clock .calendar-clock {
    border: solid;
    font-size: 2em;
    font-weight: 700;
}

.popup-clock .calendar {
    background-color: transparent;
    color: @base07;
}

.popup-clock .calendar header button {
    font-size: 1.2em;
    color: @base07;
}

.popup-clock .calendar header button {
    font-size: 1.2em;
    color: @base07;
}

.popup-clock .calendar header label {
    font-style: italic;
}

.popup-clock .calendar .day-name {
    font-weight: 700;
}

.popup-clock .calendar .day-number.today,
.popup-clock .calendar .day-number.today:selected {
    background: @base0b;
    color: @base07;
}

.popup-clock .calendar .day-number:selected {
    background-color: rgba(var(--base0e-rgb),0.6);
    outline-color: rgba(var(--base0e-rgb),0.6);
}

.popup-clock .calendar .day-number:focus {
    background: transparent;
    outline-color: @base0e;
}

.popup-clock .calendar .day-number:selected:focus {
    background-color: @base0e;
    outline-color: @base0e;
    color: inherit;
}

.popup-clock .calendar .other-month {
    color: rgba(var(--base07-rgb),0.6);
}

.workspaces {
    margin: 0;
    padding: 0;
    border-width: 0;
    background: linear-gradient(to right, @base02, @base00);
}

#dummy-widget-gradient {
    color: transparent;
    border: 0;
    margin: 0;
    padding-right: 4em;
    background: linear-gradient(to right, @base00, transparent);
}

.workspaces .item {
    padding: var(--bar-pad-vert) 0.40em;
    transition: color 400ms ease, background 300ms ease;
}

.workspaces .item.focused {
    color: @base00;
    background: @base0d;
}

.workspaces .item.urgent {
    color: @base00;
    background: @base08;
}
