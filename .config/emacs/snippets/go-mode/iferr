# -*- mode: snippet -*-
# name: error conditional
# key: iferr
# condition: (string-match-p "^[[:space:]]*iferr" (+get-line))
# --
if $1${1:$(when (> (length yas-text) 0) "; ")}err != nil {
   ${0:return err}
}
